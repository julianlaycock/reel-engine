# deps / build
node_modules/
dist/
out/
build/
.next/

# transient
tmp/
*.log
.wrangler/

# secrets
.env
.env.*
!.env.example

# heavy media (keep engine code + small assets; renders/media live with the consuming project)
*.mp4
*.mov
*.wav
*.mp3

# os cruft
.DS_Store
Thumbs.db

# whisper.cpp model weights — 147MB of binaries, fetched by install-whisper-cpp
.whisper/
graphify-out/
